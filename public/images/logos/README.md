# Operator logos

Drop logo image files here (e.g. `pmu.png`) and reference them from
`data/operators.js` using `logoImage: '/images/logos/pmu.png'`.

If `logoImage` is left empty, the operator's `name` is shown as a plain
text logo instead — no image required.
